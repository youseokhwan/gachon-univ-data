3주차 C++

58p 자료형 -> 저장 공간을 효율적으로 사용하기 위해 사용
<iostream> 예전에는 .h를 붙여서 혼용하였지만 최근에는 붙이지 않음

59p #include <iostream>
using namespace std;
<< 2줄은 외워두고 시작하라고 강의시간에 말씀하심

60p 식별자 (= 변수의 이름 등)
C언어와 선언 규칙은 같음
변수 이름 의미가 잘 통하게 작명하는 것이 더 중요

61p extra) 메모리
OS
Code
Data
Stack >> int unit, int count, int total 적재됨
Heap(?)

61p cin, cout 등은 함수가 아닌 객체 (중간고사 이후 제대로 배울 것)

extra)
변수를 선언과 동시에 항상 초기화해주는 습관을 들이는 것이 좋음
쓰레기값이 오류 불러일으킬 수 있기때문

63p C++에선 논리형이 추가

66p 소수형, 지수형 넘어감

73p 문자열형이라는 자료형 자체는 없다 (배열, 포인터 이용)

extra)
실습은 중간에 하고 수업은 3시간30분정도 진행 예정

extra)
프로젝트 여러 개 일땐 시작프로젝트 설정 주의

extra)
자료형이 다른 변수끼리 연산을 하면 더 큰 자료형으로 변환됨

70p 캐스팅 ==  형 변환

extra)
프로젝트 여러개일경우 프로젝트만 빌드하기 기능이 있음

75p
배열의 이름은 그 배열의 시작점을 의미 (포인터 역할)
cstring 헤더가 없어도 strlen 함수가 정상적으로 작동함

extra)
예약돼있는 단어들 -> 키워드 라고함 (bool, int 등)

extra)
숙제는 실제로 돌려보고 실행이 되면 손코딩으로 제출
시험은 직접 해보지않으면 못풀만한 문제로 제출 (강의시간에 말씀하심)

77p
연산자 우선순위에서는 다 외우면 좋긴하지만
( ) << 괄호가 가장 높다는 것은 확실히 기억해두는게 좋다

extra)
(괄호치는 것 매우 중요하게 생각하시는 것 같음)

93p
시프트 연산자는 연산을 할 때 쓰이고, 곱하기 나누기 대용으로 쓰임
곱하기 나누기는 시간이 상당히 오래걸림
(비트로 계산하면 효율적임)




