19.08.14(수) 지브릿지 6일차

<네트워크 프로그래밍>
- url을 이용한 http 연결 프로그래밍
- 방법 : 자바에서 기본적으로 제공하는 URL 클래스 등 사용
      or 써드파티 라이브러리 사용(Unirest 등)
- url에 데이터가 보이는 방식(GET 방식?) <-> url에 보이지 않는 방식(POST 방식)
- 크롤링 : 웹사이트의 정보를 프로그램으로 가져오는 것
         실습(가천대학교 홈페이지를 긁어와서 콘솔에 출력 -> 파일로 저장해보기)
- 웹에서 바이너리 데이터를 전송할 때 base64 같은 방식을 사용하여 텍스트로 인코딩/디코딩하기도 함
- 날씨정보 구현 프로젝트 소개(짧굵배 참고)
- unirest.io 사이트 소개


- 소켓 프로그래밍
- 클라이언트/서버 구현
- 서버에서 ServerSocket의 accept() 메소드 -> 문제가 있음(blocking 메소드)
  클라이언트가 접속할 때까지 계속 멈춰있는 메소드, 성능 하락(멀티쓰레드 메소드로 바꾸는게 좋음)
  -> NIO(Non-blocking I/O)
- 


<enum>
- 유형을 지정하는 특별한 클래스
- 오타 방지
- 중요한 정보가 문자열에 들어가는 것은 항상 바람직하지 않다. (오류가 검증하기가 힘들기 때문)
- 