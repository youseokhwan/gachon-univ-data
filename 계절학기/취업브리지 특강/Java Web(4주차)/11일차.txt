19.08.28(수) 지브릿지 11일차

<web server 에러>
1) 404 : 요청한 url에 대한 리소스가 없음, not found, 경로 문제
2) 500 : 

<JSP>
1) 선언문
2) 표현식
3) 스크립틀릿
- 셋 다 점점 영향력이 줄어들고 있으나 알고 있어야 한다

- response, request, session, page, application 등의 키워드 공부
- 트위터 비슷한 채팅 멘션? 프로그램 실습
// 아무리 심플한 프로젝트여도 디자인을 넣는 연습을 할 것. (그래야 가치가 있어보임)

- 부트스트랩은 Mobile First기 때문에 해상도 대응에 아주 유연함
// 프로젝트 할 때 참고

- 따로 DB에 연동하지 않기때문에 application scoop?에 저장. (모두가 공유하는 영역)
- session : 사용자마다 생성되는 저장 공간
- application : tomcat 전체에서 생성되는 저장 공간(공용)

- redirect : 클라이언트가 A를 호출 -> A가 아닌 B로 가도록 전환(다시 연결)
			 만약 A로 전달된 parameter가 있으면, 없어진다.
- forward : 똑같이 전환하지만 response와 request를 모두 전달한다.


// 최근엔 single 페이지로 많이 구성(한 페이지에서 글쓰기와 목록이 동시에 있는 방식)
// 페이지가 분리돼있으면 데이터 유지가 복잡하기 때문
// 프로젝트할 때 참고


<DB 연동>
- jsp 파일 자체로 실행되는 것이 아닌 .war(Web Archive Resource?) 파일이 생성되면서 패키징 됨
- 이 파일을 tomcat이 로드하면서 진행
- 만들어진 것들을 실제 os에서 구동하려면 서버 deploy(대치)하는 과정이 필요(서버가 제공하는 기능)
  tomcat에서 관리자 로그인 후 war 파일을 등록하면 가능
- lib 폴더는 war에 같이 패키징됨
  필요한 라이브러리 파일들은 lib 폴더에 있어야 함


<시험>
- 9시 정각에 시작해서 50분 정도 시험
- 어제 오늘 배운 내용 (java web)
- 코드 이해할 것(괄호 넣기, 관계성, 부분코드)
- 용어 및 핵심 개념(단답형 문제)










