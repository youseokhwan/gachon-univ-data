/*

#include <stdio.h>
int main(void)
{
	int num; // num은 사용자의 선택

	printf("가위(1), 바위(2), 보(3), 종료(0) 중 한 개 선택: "); // 컴퓨터는 바위를 낸 상태
	scanf("%d", &num);

	if (num == 1)
		printf("사용자: 가위, 컴퓨터: 바위 -> 졌습니다.\n");
	else if (num == 2)
		printf("사용자: 바위, 컴퓨터: 바위 -> 비겼습니다.\n");
	else if (num == 3)
		printf("사용자: 보, 컴퓨터: 바위 -> 이겼습니다.\n");
	else
		printf("종료합니다.\n");

	return 0;
}

*/