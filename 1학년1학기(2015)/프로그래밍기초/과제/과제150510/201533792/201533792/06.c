/*
#include <stdio.h>
int main(void)
{
	int num, min = 10000;

	while(1)
	{
		printf("데이터를 입력하시오(음수를 입력하면 종료): ");
		scanf("%d", &num);

		if(num < 0)
			break;
		else if(num < min)
			min = num;
	}

	printf("최솟값은 %d입니다.\n", min);

	return 0;
}
*/